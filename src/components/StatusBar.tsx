export default function StatusBar() {
  return <div data-testid="statusbar" style={{height:'var(--statusbar-height)',background:'var(--p1)',borderTop:'1px solid var(--b1)',flexShrink:0}}>StatusBar</div>
}
